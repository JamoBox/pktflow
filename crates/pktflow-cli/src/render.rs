//! Rendering primitives, re-exported from the shared presentation layer
//! (`pktflow-view`) so every CLI module keeps its `crate::render::` paths.
//! The FR-28 table and its unit tests live in `pktflow_view::fmt`.

pub use pktflow_view::fmt::*;
